@{
  Layout = "_Layout";
}

@model University.Models.Student  //this is where we get the 'model' which represents student class/constructor.

<h4>Add a new Student</h4>

@using (Html.BeginForm())
{
    @Html.LabelFor(model => model.StudentName)
    @Html.TextBoxFor(model => model.StudentName)

    @Html.Label("Select Course")
    @Html.DropDownList("CourseId") //this is where we use the viewbag.

    <input type="submit" value="Add new student" class="btn btn-default" />
}
<p>@Html.ActionLink("Show all students", "Index")</p>